type: object
properties:
  build_status:
    type: string
    enum:
      - in-progress
      - errored
      - failed
      - succeeded
  agent_addr:
    type: string
additionalProperties: false
required:
  - build_status
  - agent_addr
